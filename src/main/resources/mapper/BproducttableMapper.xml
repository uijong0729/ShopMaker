<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="global.sesoc.bigstar.dao.BproducttableMapper">

	<select id="selectbproducttable" resultType="Bproducttable">
		select * from
		Bproducttable where productcode =#{productcode}

	</select>

	<select id="selectallbproducttable" resultType="Bproducttable">
		select * from
		Bproducttable
	</select>

	<select id="getProductdetailListByName" resultType="Bproducttable">
		select * from
		Bproducttable where productname =#{productname}
	</select>

	<insert id="insertbproducttable" parameterType="Bproducttable">
	
		insert into BPRODUCTTABLE(productcode,productname,productprice,productquantity,productsize,productcolor,productspec,productpushdate,productdisplay,productkind,membercode) 
                    VALUES(BPRODUCTTABLE_seq.NEXTVAL,#{productname},#{productprice},#{productquantity},#{productsize},#{productcolor},#{productspec},sysdate,1,#{productkind},#{membercode}) 
	</insert>
</mapper>